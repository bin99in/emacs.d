;;; init-keyfreq.el --- Set up keyfreq -*- lexical-binding: t -*-
;;; Commentary:
;;; Code:

(use-package keyfreq
  :config
  (keyfreq-mode 1)
  (keyfreq-autosave-mode 1))

(provide 'init-keyfreq)
;;; init-keyfreq.el ends here
